const nextConfig = {
  /* config options here */
  images: {
    remotePatterns: [
      {        
        hostname: 'images.unsplash.com',
      },
      {
        hostname: 'img.freepik.com',
      }
    ],
  },
};

export default nextConfig;
